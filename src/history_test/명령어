# 입력 후 새로고침 히스토리 자동화
python -m pytest .\src\history_test\test_history_refresh.py -v -s

# 히스토리 삭제 테스트 전체
python -m pytest .\src\history_test\test_history_bulk_delete.py -v -s

# 재로그인 후 히스토리 복원 테스트만 실행
python -m pytest ".\src\history_test\test_history.py::test_history_survives_relogin" -v -s

# history_test 폴더의 전체 테스트 실행
python -m pytest .\src\history_test -v -s

#필요 모듈 설치
Get-Content requirements.txt